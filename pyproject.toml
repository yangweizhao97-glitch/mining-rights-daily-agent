[project]
name = "mining-rights-daily-agent"
version = "0.1.0"
description = "MCP-based mining rights daily brief agent for Cursor / Claude Desktop verification."
requires-python = ">=3.10"
dependencies = [
  "mcp>=1.8,<2",
  "langgraph>=0.2,<1",
  "pypdf>=4,<6"
]

